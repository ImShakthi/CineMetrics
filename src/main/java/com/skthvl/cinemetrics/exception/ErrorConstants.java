package com.skthvl.cinemetrics.exception;

/**
 * Contains constant error messages used throughout the application.
 * These constants ensure consistency in error reporting and messaging across different components.
 */
public class ErrorConstants {
  /** Error message returned when the provided API key is invalid or unauthorized. */
  public static final String INVALID_API_KEY = "Invalid API key!";
  /** Error message returned when a requested movie cannot be found in the database. */
  public static final String MOVIE_NOT_FOUND = "Movie not found!";
}
